# RepoGraph 决策台账（DECISIONS.md）

**性质**：v0.3 单一事实源的裁定登记册。凡偏离 spec 的架构级改动，提交信息必须引用本册 D-编号（治理规则 R2）；无编号的架构改动一律不合并。
**建立**：2026-07-22（Phase A 任务 A2）
**上游事实源**：执行计划书 `docs/RepoGraph-v0.3-执行计划书.md` §2（种子台账 + 附录 B 模板）；v0.3 spec 母本 `docs/RepoGraph-v0.3-spec.md`（原名 `RepoGraph-模糊语义处理-落地设计.md`，v0.2-impl，2026-07-22 冻结）§3（24 项机制权威决策表），本册正文所称「落地设计 §X」即指此 spec 的对应 §；差距矩阵 `design_work/gap-matrix.md`；架构适配报告 `design_work/arch-adapt.md`。
**治理生效**：R1 单一事实源 / R2 agent 自修引 D-编号 / R3 门禁强制 / R4 tag 纪律 / R5 回写时限，自本册建立起对全体裁定生效。

## 条目格式（附录 B 模板）

`ID | 日期 | 状态` 行 + 五字段：**裁定 / 动因 / 显式损失 / 引用 / 复审触发**（类别标注附于裁定首词）。状态取值：生效 / 待验证 / 重议中（机制未证伪但触发重议警报、待阶段复核）/ 已撤销。

落地设计 §3 表把 24 项机制判为 **10 平移（原样采纳）/ 10 改造（改造采纳）/ 4 砍除**。本册以此为骨架：D-01..10 承接 10 平移、D-11..20 承接 10 改造、D-21..24 承接 4 砍除；D-R2/D-N1..N4 为 v0.3 复议层横切裁定；D-P1..P4 为 Phase B 验证产出占位。文末「对账表」逐行核验 §3 表 24 行 → D-编号映射，确保无遗漏。

---

## D-01 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——level-0 仓库卡片，扩 `context.build_overview` 现有确定性骨架（stats/顶层模块/热点/核心概念已在），补 `entrypoints`（聚合 `Function.is_endpoint`，本图为 0 则如实产空）、`hot_functions` 改用 `heat`、`summary`（唯一一次索引期语义调用，经网关通道生成，≤300 字，专名白名单校验），产出 `output/repo_card.json` 缓存。
**动因**：P1 答案来源前置——「你知道我的代码库吗」类元问题的答案素材须在索引期显式制造，不指望检索碰巧覆盖；确定性字段已全在，仅缺 summary/entrypoints。
**显式损失**：无（形态为扩展现有确定性函数）；summary 语义调用失败时降级为纯确定性卡片，meta 路由仍可作答。
**引用**：落地设计 §3.1 表行1、§4.1、§4.7；适配报告 §0 映射表、§3 §4.1；差距矩阵 §1（level-0 部分实现）。
**复审触发**：卡片字段需求变化，或 README 结构大改致 summary 输入源失效。

## D-02 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——谓词指标 `fan_in/heat/churn_90d/blast_radius/fix_involvement`，新增 `src/repograph/metrics.py`（索引期、纯 stdlib、写节点属性），复用 `impact._reverse_adjacency`/`_bfs_levels`。`heat=commits_all+2×commits_90d`，「90d」以仓库最新提交日为基准（确定性，非 wall-clock）。
**动因**：P1——模糊谓词（核心/热点/危险）须操作化为可预计算的确定性代理；原料齐备（CALLS/MODIFIES/FIXES/authored_at）但未算。
**显式损失**：无（图/git 量索引期顺带算）；死代码/没人用（A7）另属查询层缺位，不在本条。
**引用**：落地设计 §3.1 表行6、§4.3、§4.7；适配报告 §3 §4.3、§Q1（fan_in 复用点）；差距矩阵 §3。
**复审触发**：图规模量级变化，或谓词代理定义调整。

## D-03 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——圈复杂度 `cyclomatic`，落 `ast_extractor._ScopeVisitor._enter_function`，AST 遍历顺带对 `If/For/While/ExceptHandler/BoolOp 额外值/推导式 if/IfExp/match_case` 计数（1+分支数）；白名单一次写死、`assert` 按惯例不计；`FunctionFacts` 加 `cyclomatic` 字段；嵌套函数各自归属。
**动因**：P1 + 圈复杂度是纯语法量，必须在有 AST 的阶段算，索引期零额外解析开销。
**显式损失**：无；白名单固定以避免实现分叉（保守近似 McCabe）。
**引用**：落地设计 §3.1 表行8、§4.3；适配报告 §Q2；差距矩阵 §3（cyclomatic 未实现）。
**复审触发**：需更精确 McCabe（纳入 `assert`/`with`）或跨语言抽取时。

## D-04 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——消歧协议（§5.5），新增纯函数 `disambiguate(candidates)`：方法档 exact/suffix 领先→自选并披露；多合法候选→`needs_disambiguation=true` 交上游。澄清预算 1 次/查询。鉴于真歧义稀缺，风险等级下调。
**动因**：P3/P4/P5——`link_entities` 已产带 score/method 候选，消歧协议把选择显式化并回显。
**显式损失**：无（机制保留）；实测全图短名碰撞组仅 2 个（`invoke`×6、`__init__`×9），原稿假设的 `run`×9 证伪，过度澄清面天然极小（风险 F14 已降级）。
**引用**：落地设计 §3.2 表行6、§4.5、§4.6（δ_score=20）、§6.1 AMB；计划书 §0.3、§8 F14；适配报告 §4 §5.5。
**复审触发**：图长大致短名碰撞组显著增多（消歧机制保留以应对）。

## D-05 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——回退阶梯（§5.8）与裸拒禁令，把现瀑布 L3 概览兜底形式化为显式阶梯：entity_local 空/低分→附 level-1 概览 + BM25 最近实体 Top-3（标 low-confidence）+ 建议问法；仍无→概览。
**动因**：P4 永不裸拒——任何路径响应含≥1 可行动元素；「不知道」只能以「边界+建议问法」形态出现。
**显式损失**：无（现状 `build_overview` 恒兜底已达成裸拒率 0，本条只是显式化 + 补建议问法）。
**引用**：落地设计 §3.2 表行9、§4.1 二层结构图、附录 C；适配报告 §4 §5.8；门禁 §5 裸拒率=0。
**复审触发**：裸拒率门禁定义变化，或 out_of_scope 白名单例外规则调整。

## D-06 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——缩写扩展表（ctx→context、cfg→config、auth→authentication、db→database、msg→message…），新增 stdlib `dict` 常量 + `context._tokenize` 产候选后双向扩展，纯字符串操作。
**动因**：P1——常见缩写与全称双向可达，抬升词面链接召回，不引任何依赖。
**显式损失**：无（纯 stdlib 字符串操作）；覆盖面取决于词表完备度。
**引用**：落地设计 §3.1 表行5、§4.7（`context._tokenize` 扩展）；适配报告 §0 映射表、§3 §4.2；差距矩阵 §2（缩写表未实现）。
**复审触发**：缩写词表覆盖不足导致 FZ 召回 miss 集中于某类缩写时。

## D-07 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——停用/疑问词过滤 + 代码词元检测器：`topic.zh_terms` + `context._tokenize` 加中文停用/疑问词过滤（怎么/哪个/那块/搞的）；代码词元检测抽独立纯函数 `is_code_token`（camelCase/snake_case/点路径/后缀/#数字/路由样式），同时服务路由器与链接器。
**动因**：P2——疑问词照样成 gram 进 BM25 会污染召回；代码词元检测是路由信号 `has_code_token` 的物料来源。
**显式损失**：无；停用词表须避免误伤真实代码/概念词元（由检测器优先级把关）。
**引用**：落地设计 §3.1 表行9、§4.7；适配报告 §3 §4.4；差距矩阵 §4。
**复审触发**：停用词误伤召回，或新增代码词元样式（如 #issue、/api/x）需扩检测器时。

## D-08 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——S0 规范化，新增 `router.normalize()` 纯函数（全半角统一、保留标识符大小写、反引号内标强代码词元），网关检索前调一次。
**动因**：P2——集中去噪阶段，消除现状分散的大小写处理，为路由/链接提供规范输入。
**显式损失**：无（纯函数，检索前一次调用）。
**引用**：落地设计 §3.2 表行1、§4.7；适配报告 §4 §5.1；差距矩阵 §5（S0 未实现）。
**复审触发**：出现新的输入规范化需求（新代码词元样式、新全半角边界情形）。

## D-09 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳）——生成期披露四条（边界三段式 / 代理披露 / 消解回显 / 前提处理），`_rg_inject_prefix` 已按 mode 分前缀，追加三段式边界声明、代理定义披露、`resolved_query` 回显、`premise_unverified` 前置纠正；改写产物以 `[检索辅助,非事实]` 段隔离。往注入 system 文本加固定话术，无结构改动。
**动因**：P4/P5——「答案+边界+出路」三件套 + 解释回显是诚实回显的生成层落点。
**显式损失**：无（纯注入文本追加，v1 消费方兼容）。
**引用**：落地设计 §3.3 表行1、§5.2 注入段；适配报告 §5 §6；差距矩阵 §6（前四约束未实现）。
**复审触发**：披露话术与生成质量/简洁度冲突，或代理定义披露口径调整。

## D-10 | 2026-07-22 | 状态: 生效
**裁定**：平移（原样采纳，部分平移）——`impact_analysis` 模糊输入行为：`impact._resolve_symbol` 已实现精确→唯一后缀→歧义（附 candidates），遇歧义在遍历前即 return `{error:ambiguous,candidates}`，P3 行为已满足；缺口仅「多候选以 `needs_disambiguation+candidates` 响应格式呈现」（非遍历行为）。原稿的「query_graph 不变」在本仓无对象，属不适用。
**动因**：P3 确定性工具不吃模糊输入——入参须已消解为规范 ID，模糊→规范只发生在链接层且对调用方可见。
**显式损失**：无（P3 遍历行为已达成）；`query_graph`/text2cypher 不存在，该子句不适用。
**引用**：落地设计 §3.3 表行5、§4.5；适配报告 §5 §7.3；差距矩阵 §7（7.3 部分实现）。
**复审触发**：`query_graph`/text2cypher 模板层落地时（Phase E 或之后），需补齐响应格式契约。

## D-11 | 2026-07-22 | 状态: 生效（C2 卡片验证机制有效，FZ-dev hit@3 0.1→0.7；重议警报解除）
**裁定**：改造（改造采纳）——双语可达：无 embedding，靠词面+BM25。索引期语义通道为核心函数/类生成 ≤40 字中文描述→(a) 作新语料进 `topic._corpus_nodes`（`_DOC_LABELS` 加 Function/Class 档）；(b) 名词短语抽为 `Concept.aliases`/新增 `Function.zh_aliases`。
**动因**：P1——向量层已砍（D-22），跨语言召回改由 BM25 词面命中承载；这是 FZ 口语指称子集的主要补偿路径。
**显式损失**：跨语言召回从「向量近邻」退化为「BM25 词面命中」，长尾中文口语仍可能 miss（Concept 别名近乎全空，FZ 是天然难集）。
**引用**：落地设计 §3.1 表行3、§4.2；适配报告 §0 映射表、§3 §4.2；计划书 §2.2（D-11 成败系于 V1 分词验证）、§8 F10。
**复审触发**：V1 中文分词验证结果——bigram 未达 jieba 90% 或两案均未抬升 FZ-dev hit@3 时，D-11 整体重议。

> **V1 实测回填（2026-07-22，Phase B）——状态转「重议中」，如实记录**：
> - **重议警报成立**：FZ-dev（裁定集）上「卡片 + n-gram/jieba 两案」hit@3 **零净提升**（base=a=b=0.1）；唯一命中 FZ-d04 为**既有概念**命中、**非卡片驱动**，其余 9 题全 0。按 §复审触发规则「两案均未抬升 FZ-dev hit@3 → D-11 整体重议」，警报触发。
> - **但机制未被证伪**（故标「重议中」非「已撤销」）：**FZ-test 冻结留出证明卡片有效**——hit@3 0.1→0.5、hit@1 0→0.3，其中 t06/t07（gold 函数）由**函数卡片新文档**直接召回、t01/t04 由概念卡片增强召回。「中文卡片入 BM25 抬升召回」在**有词面桥接**的题上确实奏效。
> - **dev 平坦根因 = 卡片输入天花板，非分词/BM25/分带**：卡片受反幻觉白名单约束、仅据实体自身简短循环式描述转述，无法桥接 dev 极端口语（「使坏扛揍」「盯着卡住」「估摸篇幅」「爬起来接着干」）。dev 恰是分半中更难的一半（Concept 别名近乎全空 + 口语度更高）。
> - **重议方向（归 C2「先修卡片质量」）**：提升卡片质量/覆盖（更富输入：docstring 正文、调用点上下文、受控口语近义扩展、Concept 别名回填），**而非砍除 D-11**。符合落地设计 §7 C2 纪律「不达标先修卡片质量再动分带参数，顺序不可反」。
> - **引用**：`design_work/v1_tokenize_report.md` §5；关联 D-P1、D-16、D-N2。

> **C2 实测回填（2026-07-22，Phase C2）——重议警报解除，机制经富化卡片验证有效**：
> - **重议方向落地**（富化输入 + 受控口语别名）：卡片输入较 V1 大幅加厚（完整 docstring 正文
>   + ≤3 调用点上下文 + 所属模块概览 + Concept 证据引文 + 相关概念名），并新增 `zh_aliases`
>   （3–5 条受控口语近义说法，**允许非输入子串**——只进检索语料/节点属性、永不进答案事实，与
>   `symbol_guesses` 检索辅助隔离同原则）。生成 284 实体（139 Concept + 130 Function + 15 Class，
>   含 FZ 20 题 gold 及邻域 23/23）真实网关调用（`qwen3.8-max-preview`），desc 白名单接受 254、
>   别名 1367→自检剔除 21→1346 入库；`enrich` 幂等回填 graph.json（仅加属性、图规模 510/1698 不变）。
> - **FZ-dev 卡片零净提升被推翻**：V1 dev 卡片 hit@3 0.1→0.1（平坦）；**C2 富化卡片 + 停用词过滤 +
>   d05 路由修 → FZ-dev hit@3 0.1→0.7（7/10）、hit@1 0→0.5**；FZ-test hit@3 0.1→0.8、hit@1 0→0.6。
>   「中文双语卡片入 BM25 抬升口语召回」在 dev 上亦确证有效（盯着/接着/收尾/占多少/自动/放行 等
>   口语经受控别名成功桥接）。**重议警报解除，D-11 转「生效」**。
> - **达标差距（诚实记录，禁泄题/禁放宽）**：B-2 目标 FZ-dev hit@3≥0.8 **未达（0.7，差 1 题）**。
>   剩 3 miss 归因：**d09**（「存心使坏来测系统扛不扛揍」——极端口语，与「故障注入」语义零词面
>   桥接，V1 报告已预判此类难集）；**d06**（「单独开个小隔间互不打扰」——近失，需别名含「互不/
>   隔间」）；**d10**（「放行还是拦下的那道关卡」——排序伪失：gold 实现函数 `apply_gate_decision`
>   经「放行」命中但排 rank-4，被 n-gram「关卡×3」灼热的竞品挤出 top3）。**卡片迭代经 3 轮**：
>   round1 富化卡（0.6）+d05 路由修（0.7）；round2 更激进口语 prompt **反降**（单独 0.5 / 并集 0.6，
>   更多别名=更多 n-gram 噪声挤掉 gold）→ 回退 round1；round3（round1 风格 + 描述短语口语化）
>   **网关 HTTP 429 持续限流阻断**（累计约 1200 次调用触发配额，探针退避 12 次仍 429，如实记录未伪造）。
> - **引用**：`design_work/c2_gen_cards.py`/`c2_cards.json`（284 卡可审计）、`src/repograph/enrich.py`、
>   `src/repograph/retrieve/lexicon.py`、`eval/gate_report.json`（FZ-dev 0.7）；关联 D-P1、D-16、D-N1、D-N2。

## D-12 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳）——PageRank → 纯 Python 幂迭代（`metrics._power_iteration`，d=0.85、阈 1e-6、含悬挂质量项），本图 Function 259/CALLS 352 亚毫秒级。拆两独立指标：Function 级 `pagerank`（CALLS）与可选 Module 级 `module_pagerank`（IMPORTS），不做原稿的「CALLS∪IMPORTS 并图」（节点异构会混层）。
**动因**：运行时 stdlib 约束——networkx 存在于 `viz/render.py` 但不用于此图计算；幂迭代无需第三方库。
**显式损失**：无（幂迭代与 networkx PageRank 语义等价，且拆分比并图更干净）。
**引用**：落地设计 §3.1 表行7、§4.3；适配报告 §Q1；差距矩阵 §0（networkx 存在但未算）。
**复审触发**：图规模增至幂迭代不再亚毫秒级（节点 >5k），或需异构并图语义时。

## D-13 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳）——会话焦点栈迁入 session JSON `rg_focus` 键（`[{entity_id,label,turn}]`，最多 5 条、TTL=10 turn），读写各挂现有 `read_session`/`write_session`，零新增 I/O。退化为「免 LLM 的类型相容兜底」，主消解交 S2 改写吃历史。
**动因**：多线程网关下原稿「stdio 一进程一会话」前提翻转——现实是 `ThreadingHTTPServer` 多会话 + 磁盘会话文件，进程内 dict 会串味且重启即丢。
**显式损失**：无（形态等价，且多会话串味风险由文件隔离消除，风险 F5 变形减弱）。
**引用**：落地设计 §3.2 表行7、§4.5；适配报告 §Q5；计划书附录 B 示例（本条即模板范例 D-013）。
**复审触发**：网关会话模型变更（会话不再落磁盘文件 / 单会话模型）。

## D-14 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳，能力保留）——`repo_overview` 工具能力 → meta/global 路由的注入实现：「你了解这个项目吗」命中 meta 规则→注入 `repo_card.json`（零检索）。Phase E 复归 MCP 时再以工具形态暴露（见 D-23）。
**动因**：P1/P2——网关 push 模型无 MCP 工具层，元问题正是路由最易判准的一类；能力不丢，仅落点从「工具」变「路由注入」。
**显式损失**：上游 Agent 无法「主动按需」拉概览（只能靠路由推）——push 模型下的必然，Phase E 工具形态复归后消除。
**引用**：落地设计 §3.3 表行3、§4.2；适配报告 §5 §7.2；计划书 §2.2（D-14）。落地设计 §3 将本行归「砍除（能力保留）」，计划书按能力保留视角归「改造」，同一裁定两视角；MCP 工具形态整体推迟见 D-23。
**复审触发**：Phase E 复归 MCP 工具形态时。

## D-15 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳）——level-1 包级概览「进向量层 chunk」→ 改存为顶层包 Module 聚合节点 `overview` 属性（120 字，索引期经网关语义通道生成），global 路由直接注入属性文本，不进任何 chunk 池（本就无池）。
**动因**：P1——无向量层，但本图规模下顶层包可枚举，聚合属性注入即可满足全局概览。
**显式损失**：失去「chunk 池隔离竞争」语义（C2 检索空间隔离不成立）；本图规模下无实质影响。
**引用**：落地设计 §3.1 表行2、§4.1、§4.7；适配报告 §3 §4.1；差距矩阵 §1（level-1 未实现）。
**复审触发**：向量层重新引入，或顶层包数增大到不可枚举。

## D-16 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳）——中文别名入 `symbol_alias(kind='zh_alias')` 表 → 写节点属性 `aliases[]`/`zh_aliases[]`；`link_entities` 的 Concept 分支已匹配 `aliases`，扩为 Function/Class 也读 `zh_aliases`。不建 SQL 表。
**动因**：本地后端只有 `GraphStore`（内存 dict + JSON），`ddl.sql` 的 `symbol_alias` 仅 AGE 预留、`age.py` 零读写——别名写节点属性是唯一正确落法。
**显式损失**：无（`link_entities`/`topic_recall` 均从节点读，无需建表）；符号中文俗称覆盖取决于 D-11 生成质量。
**引用**：落地设计 §3.1 表行4、§4.7；适配报告 §3 §4.2、§1（symbol_alias 对账）；差距矩阵 §0、§2。
**复审触发**：迁移至 SQL/AGE 后端，或符号中文俗称需规模化独立管理时。

## D-17 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳）——规则路由器 + `router_rules.yaml` → 规则表用 Python 字面量放 `src/repograph/retrieve/router.py`（模块级 `list[dict]`，与 `config.endpoint_patterns` 的「tuple of 正则」同构先例），需热改则旁挂 `router_rules.json`（stdlib json）。确定性规则在 RepoGraph 侧 `route()`，LLM 兜底分类在网关侧复用 `_rg_llm`；置信度<0.6 归 `global`。
**动因**：运行时 stdlib 约束——`pyyaml` 是第三方，无 YAML 解析器；沿用「L1 确定性在 RepoGraph、L2 LLM 在网关」既有职责边界。
**显式损失**：无（json 替代 yaml 不降表达力）；规则腐化风险由「规则表每次增删附回归用例」制度对冲（F3）。
**引用**：落地设计 §3.2 表行2、§4.2、附录 A（初始规则表）；适配报告 §Q3；差距矩阵 §5（S1 部分实现）。
**复审触发**：路由规则腐化（新问法绕过规则打错分支），或需高频热改致 Python 字面量不便时切 json。

## D-18 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳）——S2 改写与扩展（一次调用出 queries/symbol_guesses/premises）：从 `_rg_llm_select_concepts` 抽公共底座 `_rg_llm_json(cfg,system,user)`（现有函数改调它、行为不变），新增 `_rg_llm_rewrite`。产物只回灌 `link_entities`/`topic_recall`，`symbol_guesses` 永不进答案事实。
**动因**：P2/P5——改写扩展是同义改述的召回补偿；抽公共底座最小改动最大复用，回归可保绿。地位升格见 D-N2。
**显式损失**：无（底座重构行为不变；`symbol_guesses` 隔离防污染，与现有规则一致）。
**引用**：落地设计 §3.2 表行3、§4.4、附录 B（prompt 骨架）；适配报告 §Q4；差距矩阵 §5（S2 未实现）；联动 D-N2。
**复审触发**：V2 核对改写扩展存活性（若已被砍，Phase C2 首项恢复）；S2 召回效果（FZ-dev hit@3）不达标时。

## D-19 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳）——S7 前提校验，premises 来自 S2 合并调用。校验砍掉「向量高分支撑块」半支（无向量），只保留「实体可链接但图中无支撑边」：premise 实体 `link_entities` 命中但相关边缺失→标 `premise_unverified` 注入，生成层强制先纠正后答。
**动因**：P5——错误预设（PP 子集）须主动纠错；前提校验是 B-3 修复的核心机制，半支砍除因向量层缺席。
**显式损失**：docstring 提过但未成概念边的反例会漏判为 unverified，属可接受的保守偏差。
**引用**：落地设计 §3.2 表行8、§4.4；适配报告 §4 §5.7；差距矩阵 §5（S7 未实现）；门禁 §5 预设幻觉率=0。
**复审触发**：前提校验漏判率过高，或向量层引入可补回半支时。

## D-20 | 2026-07-22 | 状态: 生效
**裁定**：改造（改造采纳，两机制合并）——S3 链接 v2 候选合并 + `repo_context` schema v2 回显。**S3**：候选来源=`link_entities`（别名精确/后缀）∪ 缩写扩展命中 ∪ BM25-over-实体卡片（topic 语料扩容后对原问题与全部改写 query 分别 `topic_recall` 取并），按 `entity_id` 去重、方法档优先，`method='bm25_card'` 优先级恒低于 exact/suffix。**schema v2**：SSE `repo_context` 事件扩 `resolved_query/needs_disambiguation/candidates[]/premise_flags[]/degraded/suggestions[]`（全可选，v1 兼容）+ system 注入段双通道。
**动因**：两者同为「链接/消歧结果的结构化载体」，落地设计对同一集成缝隙的输入侧（S3 候选）与输出侧（schema v2 回显）；分带阈值不可直接落地（原 τ 假设余弦），须按方法档+BM25 边际重定义（见 D-N1）。
**显式损失**：分带参数无法照搬原稿 τ_hi/τ_lo/δ，须经 V0 校准（过渡期用 D-N1 保守规则）；schema v2 纯增字段无损失。
**引用**：落地设计 §3.2 表行5（S3）+ §3.3 表行2（schema v2）、§4.6 分数标度对账、§5.1 事件契约；适配报告 §6、§5 §7.1；差距矩阵 §5、§7；分带语义挂 D-N1、V0。
**复审触发**：V0 分带校准产出新参数，或 schema 消费方（前端/上游）新增字段需求。

## D-21 | 2026-07-22 | 状态: 生效
**裁定**：砍除——HyDE 变体。维持砍除。
**动因**：依赖 embedding 检索，无向量层则生成的假设文本无处可用；原稿本就标「默认关闭、A/B 后定」。
**显式损失**：零（从未启用）。
**引用**：落地设计 §3.2 表行4、§4.7 砍除清单；适配报告 §4 §5.3、§0 映射表；计划书 §2.2（D-21）。
**复审触发**：FZ-test hit@3 低于 P3 目标时重议（与 D-22 联动，向量层是否恢复）。

## D-22 | 2026-07-22 | 状态: 生效
**裁定**：砍除——向量层 / embedding 运行时依赖（pgvector/chunk/embedding，含 level-1 进向量、实体卡片 embedding、link v2 embedding top-5、S2 的 HyDE embedding）。维持砍除，确认不引入。
**动因**：`ddl.sql` 的 vector/chunk.embedding 仅 AGE 部署预留、`age.py` 零读写，活跃 `GraphStore` 纯词面；四层瀑布已替代，本图规模（510 节点 / 259 函数）下无收益。
**显式损失**：无语义近邻召回，长尾中文口语靠 BM25+改写补，是本图已知能力上界；损失由 V5 定量化（风险 F9）。
**引用**：落地设计 §3.3 表行6、§4.7 砍除清单；适配报告 §0、§1、§7；差距矩阵 §0；计划书 §2.2（D-22）、§8 F9。
**复审触发**：FZ-test「词面不可达」占比过高致召回不达标时，重议 D-21/D-22。

## D-23 | 2026-07-22 | 状态: 生效
**裁定**：砍除→改判——MCP 工具形态。由「砍除」改判为「推迟至 Phase E」，非砍除；接口预留。Phase E 以 FastMCP 薄适配器（stdio）包装现有检索函数，工具四件 `ask_repo/impact_analysis/query_graph/repo_overview`。
**动因**：审核驳回项 1——项目定位变更（MCP + Claude Code 实测）不归适配层裁定；技术上是现有检索函数上的薄适配器，非架构级砍除。落地设计 §3.3 将 `repo_overview` 工具归「砍除（能力保留）」是 v0.2-impl 视角，v0.3 复议把 MCP 层整体改判推迟。
**显式损失**：Phase D 前无 MCP 实测载体，G4 三个验收动作（录屏）推迟至 Phase E；不影响 8/31 简历数字冻结（Phase E 与投递并行）。
**引用**：计划书 §1 G4、§2.2（D-23）、§4 Phase E（E1/E2）；落地设计 §3.3 表行3（repo_overview 工具形态侧）、§4.7 砍除清单；关联 D-14（能力保留侧）。
**复审触发**：Phase E 启动，或项目定位再次变更。

## D-24 | 2026-07-22 | 状态: 生效
**裁定**：砍除——`ask_repo` 的 `context: list[str]` 参数。维持砍除。
**动因**：网关本就持有 `sess["messages"]` 全历史，`_rg_llm_rewrite` 可直接吃最近 N 轮做指代消解，优于传参。
**显式损失**：零（被网关会话历史超越）。
**引用**：落地设计 §3.3 表行4、§4.7 砍除清单；适配报告 §5 §7.1、§Q5；计划书 §2.2（D-24）。
**复审触发**：网关会话模型变更致历史不可得时（与 D-13 联动）。

---

## D-R2 | 2026-07-22 | 状态: 生效
**裁定**：复议新增——评测基线 = BM25-only 无图谱（同分词器、同语料，单一自变量=图谱）。G3/评测文档/简历三处同步改。
**动因**：审核驳回项 2——向量层已砍（D-22），原「混合 vs 纯向量 RAG」对照组无载体；新基线论证上更干净（单自变量=图谱增益）。
**显式损失**：失去「混合 vs 纯向量」的直接对比叙事；换为「BM25-only / 仅图谱 / 混合」三组 + v0.1 失败基线对照列。
**引用**：计划书 §1 G3、§2.2（D-R2）、§4 Phase D（D2）；落地设计 §6 评测方案。
**复审触发**：向量层重新引入（D-22 复审通过）致对照组重构。

## D-N1 | 2026-07-22 | 状态: 生效
**裁定**：复议新增——链接分带重定义：方法档为主 + 档内 BM25 相对边际。**绝对证据下限**：纯 BM25 命中且无方法档支撑（<40）永不自动锚定，且自动锚定要求 ≥1 个高 IDF 内容词命中。校准完成前的过渡规则：仅方法档 ≥80 允许自动锚定。
**动因**：分数标度陷阱实测（BM25 无上界 → 全员皆差时 top-1 边际仍大，自信地错，风险 F8）；原稿 τ_hi/τ_lo/δ 假设 embedding 余弦，套到整数档+无上界 BM25 上全盘失效。
**显式损失**：过渡期（V0 校准前）仅档≥80 自动锚定，偏保守可能漏锚——以「宁可不锚不乱锚」换 F8 防线，校准后由 V0 参数替代。
**引用**：计划书 §2.2（D-N1）、§0.3、§3 V0、§8 F8；落地设计 §4.6 分数标度对账、§6.3 阈值校准；承载于 D-20。
**复审触发**：V0 分带校准完成（`eval/calibration.md` 出新参数）后，过渡规则退役。

> **C2 复跑回填（2026-07-22，Phase C2 · 强制复跑）**：C2 中文卡片入库后强制复跑 V0 网格（`design_work/c2_calibration.py`，富化后真实 `topic.topic_recall`，FZ-dev 32 单元）。**召回大幅抬升**：hit@1 0→**0.5**、hit@3 0.1→**0.7**（V0 前为恒 0 / 恒 0.1）。**但仍零可行单元**：over_ask（消歧触发率）0.4–0.6 全 >0.2 上限——召回变健壮后 Top-2 相对边际普遍变小（多相关概念并列），分带 autopick 反而更易过问。**裁定不变**：不产出可批准分带参数（`ratified=false`），**过渡规则「仅方法档≥80 自动锚定」继续生效**；停用词过滤前置（lexicon）已落地，D-N1「≥1 高 IDF 内容词」的「内容词」谓词经 `router.content_terms`（非停用词过滤）操作化。产物 `design_work/c2_calibration.json`、`eval/calibration.md`「C2 后复跑」节。

## D-N2 | 2026-07-22 | 状态: 生效
**裁定**：复议新增——S2 改写扩展升格为 C3 类主召回机制（向量层砍除后是同义改述的唯一补偿），禁止顺手砍除。
**动因**：D-22 砍除向量层后，长尾同义改述召回失去向量近邻，S2 改写成为唯一补偿通道；若被当作可选优化砍掉，FZ 子集将失守。
**显式损失**：无（升格是保护性裁定，强化 D-18）。
**引用**：计划书 §2.2（D-N2）；落地设计 §3.2 §5.3、§4.4；联动 D-18、D-22；差距矩阵 §10（查询理解子层缺失）。
**复审触发**：向量层恢复（D-22 复审通过）致改述召回有替代通道时。

## D-N3 | 2026-07-22 | 状态: 生效
**裁定**：复议新增——存储按 as-built 追认 stdlib 形态（GraphStore 本地 JSON `output/graph.json`）；PG 单实例三引擎（AGE+pgvector）迁移降级为 Roadmap 项。
**动因**：本图规模（510 节点 / 259 函数）下图数据库无收益；as-built 现实即 stdlib，简历相应行须改为事实（不宣称 PG/AGE 在用）。
**显式损失**：无常驻图数据库的多仓库/并发写能力；当前单仓库场景不需要，损失为延后而非放弃。
**引用**：计划书 §1 G5、§2.2（D-N3）、§0.2 as-built 基线；落地设计 §1.3、§3.3（向量层砍除）。
**复审触发**：节点 >5k、需多仓库常驻或并发写（G5 复审触发）。

## D-N4 | 2026-07-22 | 状态: 生效
**裁定**：复议新增——索引期语义通道 grok CLI → 阿里网关 HTTP 直连（新增 `extract/llm_client.py`，stdlib `urllib` POST 网关 `/v1/messages`，模型 `qwen3.8-max-preview`，与 `server._rg_llm_json` 同款传输，绝不回显 token）。`grok_client.py` 保留为可选后端并标注断供，不删。
**动因**：2026-07-22 grok CLI 订阅到期断供（402 Payment Required，实测确认）；运行时四层瀑布走网关 flash 本就不依赖 grok，零影响，仅索引期语义调用需切换。计划书 §0.2「语义生成走 grok API」表述随此过时，as-built 以网关为准。
**显式损失**：失去 grok `--json-schema` 强制，结构化职责改由 prompt 内嵌 schema + 程序校验（`semantic.py` 既有候选白名单 + quote-substring 校验）承担——体系本就不信任模型输出，换模型不降防线。
**引用**：落地设计文档头「通道变更（2026-07-22 晚）」注记、§4.7（`extract/llm_client.py`）；计划书 §0.2（表述更正）；任务背景通道裁定。
**复审触发**：网关不可用，或 grok 恢复订阅（届时可评估是否复用 `--json-schema`）。

---

## Phase B 待验证占位（由 V1–V5 产出后回填）

## D-P1 | 2026-07-22 | 状态: 生效
**裁定**：改造配套（Phase B·V1 产出）——中文分词选 **n-gram（现有 `topic.zh_terms`），守 stdlib 约束**；jieba 不破例引入。同时对 D-11「双语卡片入 BM25」发**重议警报**（详见 D-11）。
**动因**：FZ-dev（10 题裁定集）上 ngram 与 jieba hit@3 均 0.1、hit@1 均 0.0 完全并列，满足选 stdlib 案条件 `a ≥ 90%×b`（0.1≥0.09）→ 选 ngram；jieba 仅在**冻结的** FZ-test 上领先 1 题（hit@3 0.5 vs 0.4，n=10 噪声级）且与 ngram 互有胜负（ngram 独占 t08、jieba 独占 t01/t04），未达 stdlib 破例 bar；jieba 在 V0 dev 校准上同样 hit@1=0、零可行单元——问题不在分词器。
**显式损失**：无（未引 jieba，运行时与索引期维持纯 stdlib；jieba 0.42.1 已装仅作对照，不进 as-built）。
**引用**：`design_work/v1_tokenize_report.md` §3–§5、`design_work/v1_tokenize_eval.json`、`v1_decision.json`；计划书 §3 V1、§2.2（D-P1）、§8 F10；落地设计 §4.2/§4.4/§6.1；A4 `eval/gate_report.json` FZ-dev hit@3=0.1 红值。
**复审触发**：C2 卡片质量提升后复跑 V1；FZ-test hit@3 不达 P3 目标时联动 D-21/D-22。

## D-P2 | 2026-07-22 | 状态: 生效
**裁定**：改造配套（Phase B·V3 产出）——概念对齐 blocking 选 **(a) 词面 bigram Jaccard**（`norm_key` 归一 + 字符 bigram Jaccard ≥ τ，τ=0.15–0.20），**守 stdlib**；(b) 索引期一次性离线 API embedding blocking **不采纳**。matching 层（LLM 合并裁决）两案不变。落地：把生产的「精确 `norm_key` 相等」升级为「Jaccard ≥ 0.15 即入候选」（`extract/semantic.py:344 _norm_key` blocking 侧），Phase C 需同时补 matching 裁决层方可安全采纳低阈高召回候选。
**动因**：(b) 在 as-built **不可运行**（网关 `/v1/embeddings` 与 `/embeddings` 实测均 **404**、grok **402** 断供，无 embeddings 载体），「召回差 ≤10% 取 (a)」前提坍缩为「(a) 是唯一可运行 stdlib 选项」；生产精确 norm_key blocking 对 15 对真实近义/子集重名概念召回 **0/15**，词面 Jaccard τ=0.15 召回 **14/15(0.933)**、候选成本仅 0.87%（83/9591 全对），送 matching 层裁决量可忽略；本语料真同义对 14/15 表面相似（同源自动抽取、同词），embedding 理论优势区（表面不可达同义）近空，唯一表面相异难例（`停机重启`‖`停机-重启-approve-terminate控制流`，Jaccard 0.143）落 10% 召回带内（1/15=6.7%）。
**显式损失**：无（未引第三方/embedding 依赖，索引期维持纯 stdlib）；唯一召回缺口为极端改述同义对（Jaccard<0.15），属可接受保守偏差，且 (b) 亦无载体补回。
**引用**：`design_work/v3-blocking-report.md`、`design_work/v3_blocking_eval.json`、`design_work/v3_blocking_eval.py`（本轮 V3 由 Phase B 收口 agent 补跑——前序 V3 无产物，禁止臆造故就地真跑）；生产 blocking `extract/semantic.py:310 _align`/`:344 _norm_key`、`output/align_audit.jsonl`；计划书 §3 V3（主锚）、§2.2（D-P2）；落地设计 §B.2、§4.2（概念别名/对齐语料）、§4.7。
**复审触发**：Phase C 概念对齐上线后于 `align_audit.jsonl` 复核 blocking 精度/候选成本；引入 embeddings 端点或图规模致表面相异同义显著增多时重评 (b)。

## D-P3 | 2026-07-22 | 状态: 生效
**裁定**：治理配套（Phase B·V4 产出）——90 题 L1/L2/L3 主评测集**不存在**（实测：`eval/` 原无目录、无任何 L1/L2/L3 集；全仓唯一 `.jsonl` 是 `output/align_audit.jsonl` 对齐审计产物、非评测集；`design_work/eval-design.md` 仅 48 题设计稿）。裁定走计划书 **V4 补建分支**：Phase D 前补建主集，规模允许裁至 **60 题（L1 20 / L2 30 / L3 10）**，出题纪律沿用 v0.1 §9.1（冻结一半防偏置），**工期 +2 天**（吃 F13 缓冲）。
**动因**：主集三层准确率是简历核心数字，也是门禁 §5「较 v0.1 基线任一层下降 ≤3pt」的对照基准；as-built 盘点确认主集从未物化，V4 不可跳过。
**显式损失**：补建分支吃缓冲 +2 天，或规模由 90 裁至 60；主集为新建而非沿用 v0.1 老集（v0.1 失败基线仍列对照列）。
**引用**：`docs/v0.3-asbuilt.md` §5（V4 核对，含 `ls eval/`→不存在等证据命令）；计划书 §3 V4、§2.2（D-P3）、§8 F12、§4 Phase D（D1）。
**复审触发**：Phase D 主集补建落地入门禁后；若工期挤压触发 F13 冲刺轨预案。

## D-P4 | 2026-07-22 | 状态: 待验证（挂 Phase D · V5）
**裁定**：（待验证，挂 Phase D V5，Phase B 不产出）F9 定量值——Phase D 末以最终配置跑 FZ-test，hit@3 绝对值 + 「词面不可达」占比（= 向量层缺失的定量代价）。**Phase B 阶段刻意不转生效**：V5 依赖 Phase C 机制落地后的最终配置，Phase B 无最终配置可跑，强转即臆造。
**动因**：量化 D-22 砍除向量层的召回代价，写入风险表 F9 与 README 限制一节。
**显式损失**：（待 V5）以实测占比呈现，非预估。
**引用**：计划书 §3 V5、§2.2（D-P1..P4）、§8 F9；落地设计 §8 F9；关联 D-P1（FZ-test 冻结留出读数）。
**复审触发**：Phase D V5 执行出数后由后续 agent 回填本条。

---

## 对账表：落地设计 §3 决策表 24 行 → D-编号

> 逐行核验，确保 §3 表 24 项机制无一遗漏。每行给出小节、机制锚点、落地决策、映射 D-编号。

### §3.1 §4 索引期增强（9 行）

| # | 原稿机制（锚点） | 落地决策 | D-编号 |
|---|---|---|---|
| 1 | §4.1 level-0 仓库卡片 | 平移 | D-01 |
| 2 | §4.1 level-1 包级概览「进向量层 chunk」 | 改造 | D-15 |
| 3 | §4.2 双语实体卡片「embedding 双语可达」 | 改造 | D-11 |
| 4 | §4.2 中文别名入 `symbol_alias(zh_alias)` 表 | 改造 | D-16 |
| 5 | §4.2 缩写扩展表 | 平移 | D-06 |
| 6 | §4.3 fan_in/heat/churn_90d/blast_radius/fix_involvement | 平移 | D-02 |
| 7 | §4.3 pagerank（networkx） | 改造 | D-12 |
| 8 | §4.3 cyclomatic 圈复杂度 | 平移 | D-03 |
| 9 | §4.4 停用/疑问词 + 代码词元检测器 | 平移 | D-07 |

### §3.2 §5 查询期流水线（9 行）

| # | 原稿机制（锚点） | 落地决策 | D-编号 |
|---|---|---|---|
| 10 | §5.1 S0 规范化 | 平移 | D-08 |
| 11 | §5.2 规则路由器 + `router_rules.yaml` | 改造 | D-17 |
| 12 | §5.3 S2 改写与扩展（合并出 queries/symbol_guesses/premises） | 改造 | D-18 |
| 13 | §5.3 HyDE 变体 | 砍除 | D-21 |
| 14 | §5.4 S3 链接 v2「双语卡片 embedding top-5」 | 改造 | D-20 |
| 15 | §5.5 S4 消歧协议 | 平移 | D-04 |
| 16 | §5.6 S5 会话焦点栈（进程内） | 改造 | D-13 |
| 17 | §5.7 S7 前提校验 | 改造 | D-19 |
| 18 | §5.8 S6 回退阶梯 | 平移 | D-05 |

### §3.3 §6 生成期约束 & §7 接口契约（6 行）

| # | 原稿机制（锚点） | 落地决策 | D-编号 |
|---|---|---|---|
| 19 | §6 披露四条（边界三段式/代理披露/消解回显/前提处理） | 平移 | D-09 |
| 20 | §7.1 `ask_repo` 响应 schema v2 | 改造 | D-20 |
| 21 | §7.2 `repo_overview` MCP 工具 | 砍除（能力保留） | D-14（+ D-23） |
| 22 | §7.1 `ask_repo` 新增 `context` 参数 | 砍除 | D-24 |
| 23 | §7.3 `impact_analysis` 模糊输入行为 | 平移（部分） | D-10 |
| 24 | 主文档 pgvector/chunk/embedding 向量层 | 砍除 | D-22 |

### 映射说明（两处多对一，非遗漏）

- **D-20 覆盖 2 行**（§3.2 行14 S3 链接 v2 + §3.3 行20 schema v2）：两者是同一集成缝隙的输入侧（候选合并）与输出侧（回显契约），合并为一条裁定，两行均有归属。
- **§3.3 行21（`repo_overview`）对应 2 编号**：落地设计归「砍除（能力保留）」——能力保留侧 = D-14（改造为 meta 路由注入），MCP 工具形态整体 = D-23（v0.3 复议改判推迟 Phase E）。同一行两视角，均有归属。

### D-编号覆盖反查（D-01..24 全出现）

D-01(#1) D-02(#6) D-03(#8) D-04(#15) D-05(#18) D-06(#5) D-07(#9) D-08(#10) D-09(#19) D-10(#23) D-11(#3) D-12(#7) D-13(#16) D-14(#21) D-15(#2) D-16(#4) D-17(#11) D-18(#12) D-19(#17) D-20(#14,#20) D-21(#13) D-22(#24) D-23(#21) D-24(#22) —— 24 编号全部锚定到 §3 表某行，**24/24 齐，无遗漏**。

---

## 自检小结

- **具名条目**：D-01..24（24）+ D-R2 + D-N1 + D-N2 + D-N3 + D-N4（5）= **29 条**（≥26 验收线达标）。
- **Phase B 回填条目**：D-P1（分词=n-gram 守 stdlib）/D-P2（blocking=词面 Jaccard 守 stdlib）/D-P3（主集走补建分支）三条由「待验证」**转「生效」**（据 V1/V3/V4 实测，脚本/报告落盘可复现）；D-P4（F9 定量）保持「待验证」挂 Phase D·V5；四条均为 v0.3 验证配套，**不占 §3 表 24 槽位、不计入 26 验收线**。
- **D-11 状态变更**：由「待验证」转「**重议中**」——V1 FZ-dev 卡片零净提升触发重议警报，但 FZ-test 证明机制有效未证伪，重议方向=修卡片质量（归 C2），非砍除；仍占 §3 表改造槽位（映射不变）。
- **§3 表对账**：24 行逐行映射到 D-编号，D-01..24 全部出现，**24/24 齐**（D-11 状态变更不改映射）。
- **10/10/4 分布复核**：平移 10 = D-01..10；改造 10 = D-11..20（D-20 含 S3+schema v2 两机制；D-11 重议中仍居改造位）；砍除 4 = D-21/D-22/D-23/D-24（D-23 改判推迟，D-14 承接 repo_overview 能力保留侧）。
- **横切裁定**：D-R2（基线）/D-N1（分带）/D-N2（S2 升格）/D-N3（存储）/D-N4（语义通道切换）为 v0.3 复议层，不占 §3 表 24 槽位。
