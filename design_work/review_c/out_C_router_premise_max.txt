**router.py:439-441** — 注释块声称「可定位但断言的支撑关系缺失 → `unverified`。两者都落 flag」，与契约（可定位→不产 flag）及本函数实际行为（540-541 `if present: continue`）直接矛盾。触发场景：后续维护者据注释"修正"代码为可定位也落 flag，导致误伤真前提。修法：将该两句改为「可定位但支撑边缺失 → 本轮词面校验保守视为已获证据、不产 flag（missing_edge 留待边级校验）」，与 528-529 docstring 及 spec §5.7 对齐。

其余逻辑（字段名/取值域/返回结构/两路 source/无假数据/无密钥）与契约吻合，未发现其他缺陷。
